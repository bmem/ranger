%h1
  New credit delta
  - if @credit_scheme
    in #{link_to_record @credit_scheme}

= render 'form'

- if @credit_scheme
  = link_to 'Back', event_credit_scheme_credit_deltas_path(@credit_scheme.event, @credit_scheme)
