= semantic_form_for @callsign do |f|
  = f.inputs do
    = f.input :name
    = f.input :status, as: :select, collection: Callsign::STATUSES
    = f.input :note

  = f.actions do
    = f.action(:submit)
