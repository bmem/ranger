%p#notice= notice

%p
  %b Event:
  = @credit_scheme.event
%p
  %b Name:
  = @credit_scheme.name
%p
  %b Base hourly rate:
  = @credit_scheme.base_hourly_rate
%p
  %b Description:
  = @credit_scheme.description
%p
  %b Positions:
  %ul
    - @credit_scheme.positions.order(:name).each do |p|
      %li= p
%p
  %b
    = link_to 'Deltas', event_credit_scheme_credit_deltas_path(@credit_scheme.event, @credit_scheme)
    = link_to 'New', new_event_credit_scheme_credit_delta_path(@credit_scheme.event, @credit_scheme)

= link_to 'Edit', edit_event_credit_scheme_path(@event, @credit_scheme)
\|
= link_to 'New', new_event_credit_scheme_path(@event)
\|
= link_to 'Back', event_credit_schemes_path(@event)
