%h1
  Listing credit schemes
  - if @event
    for #{link_to_record @event}

%table
  %tr
    - unless @event
      %th Event
    %th Name
    %th Base hourly rate
    %th Description

  - @credit_schemes.each do |credit_scheme|
    %tr
      - unless @event
        %td= credit_scheme.event
      %td= link_to_record credit_scheme
      %td= credit_scheme.base_hourly_rate
      %td.text-attribute= credit_scheme.description

%br

- if @event
  = link_to 'New Credit scheme', new_event_credit_scheme_path(@event)
